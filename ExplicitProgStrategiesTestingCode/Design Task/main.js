
// Generates a ranked list of most likely completions
// based on the users' current text.
// currentText (String): text the user has entered
//                       e.g., The quick brown fox jumped over the lazy dog.
// completionWord (String): current word the user has begun typing
//                       e.g., la
// returns String []: possible completions, with best completions first
function generateCompletions(currentText, completionWord)
{


}

// TESTS
describe("Test completions ", function() {
    it("Test Example", function() {
        expect(true).toEqual(true);
    });
    it("A second test", function() {
        expect([1, 2, 3]).toEqual([1, 2, 3]);
    });

    // INSERT TESTS HERE

});